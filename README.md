# Brandon Stewart Academic Website

Local Hugo source lives in `hugo-site/`. The GitHub Pages workflow builds that directory and deploys only after changes are pushed to `main`.

Nothing is public from this local working tree until it is committed and pushed.
